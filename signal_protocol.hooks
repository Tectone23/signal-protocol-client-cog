hook:      
  namespace: "Message Client"
  source_path: "src/"
  depends: ["signal_protocol"]
  actions:
      signal_protocol_wrapper:
        "signal_protocol_wrapper"
      login_or_register:
        "login_or_register"
      send_message:
        "send_message"
      get_users:
        "get_users"
config:
  example_api_url: ""